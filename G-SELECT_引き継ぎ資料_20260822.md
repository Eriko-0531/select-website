# G-SELECT Webサイト 引き継ぎ資料

作成日：2026-08-22

## 概要

「G-SELECT」ページ群のうち、以下を新規に作成・調整しました。

- **TOPページ**：`index_sample_pattern3.html`（「ハイエンド・ミニマル」方向のデザイン案）
- **TASTE STYLE**：一覧ページ `taste_style.html` ＋ 詳細ページ10種（`style_cool.html` 〜 `style_japanese.html`）
- **STORY STYLE**：一覧ページ `story_style.html` ＋ 詳細ページ8種（`style_relax.html` 〜 `style_play_wellness.html`）
- **SELECT ITEM**（`product_sample.html`）：デザイン自体はエンジニア様が元々作成されたものです。今回はヘッダー・ナビ部分のみ他ページと統一し、**カード・スライダー等の見た目（CSS）は変更していません**。全体デザイン（TOP/TASTE/STORY側の新しい配色・レイアウト）に合わせる作業は、共有CSSへの影響範囲を判断いただく必要があるため、あえてこちらでは行わず、エンジニア様の判断にお任せしたいと考えています。

デプロイ時のファイル名リネーム規則（`index_sample.html`→`index.html`等）は、以前お伝えした通りです。

---

## デプロイ時にご確認いただきたい点

### 1. 共有CSS（`g-select_sample.css`）への影響範囲
今回、TOP/TASTE/STORYページのデザイン調整のために `g-select_sample.css` に以下を追加しています。

- `.case-grid`：列数を4列→5列に変更
- `.reveal-block`：スクロールでのフェードイン用に新規追加
- ブランドカラー（ブルー `#6abadc` / オレンジ `#e9b779`）を使った新規クラス

このファイルは本番サーバー上で**他のページからも共有されている可能性がある**とのことでしたので、アップロード前に「今回リストアップしたページ以外にも影響がないか」をご確認いただけますと安心です。既存のクラス名は極力変更せず、新規クラスの追加にとどめるようにはしています。

### 2. `images/` フォルダの扱い
- `images/taste/<カテゴリ名>/` `images/story/<カテゴリ名>/` の中に、各カテゴリの施工事例写真（`01.jpg`〜）が入っています。フォルダ構造ごとアップロードが必要です。
- **`images/_originals_backup/` フォルダはアップロード不要です。** 写真整理時に生じた元データ（お客様邸名などが入ったファイル名のもの）の退避用バックアップで、公開する意図はありません。

### 3. CSSのバージョン管理（キャッシュ対策）
`<link rel="stylesheet" href="g-select_sample.css?v=...">` の `?v=` の数字がページによってバラつきがあります（統一のご確認をお願いします）。本番反映時にキャッシュが古いままにならないよう、更新のたびに番号を上げていただくことをおすすめします。

### 4. SELECT ITEM（`product_sample.html`）のGoogleスプレッドシート連携について
このページは商品データをGoogleスプレッドシートから取得して表示する仕組みになっています（HTMLに直接商品を書き込む方式ではありません）。

- スプレッドシート側で「アイテム」「ギャラリー」「リンク」の3タブを管理しており、それぞれ「ウェブに公開」設定でCSVとして公開しています。
- ページ内の `SHEET_URLS` という変数に、その公開CSVのURLが直接書き込まれています。
- スプレッドシートの内容を編集すると、数分後に本番サイトの表示にも反映される仕組みです（コード変更は不要）。
- **注意：この仕組みは `file://` で直接開くと動作しません**（Google側のCORS設定により、`http://` または `https://` 経由でアクセスする必要があります）。ローカルで確認する際は簡易サーバー経由で開いてください。
- 本番サーバーにアップロードした後、実際にスプレッドシートの内容が正しく表示されるか、**一度必ず動作確認をお願いします**（外部通信が絡む部分のため、ローカルでの確認だけでは本番環境での挙動を保証できません）。

### 5. 想定される軽微な不具合
ローカルの簡易サーバーでの動作確認のみ行っており、本番サーバー実機での検証はしていません。アップロード後、下記のような点で微調整が必要になる可能性があります。

- 相対パスで参照している画像・CSS・JSファイルの配置漏れ
- サーバー環境によるファイル名の大文字・小文字の扱いの違い
- 上記の共有CSSへの影響

---

## ファイル一覧（今回の作業範囲）

| ファイル | 内容 |
|---|---|
| `index_sample_pattern3.html` | TOPページ（デプロイ時は`index.html`にリネーム） |
| `taste_style.html` / `story_style.html` | TASTE STYLE / STORY STYLE 一覧ページ |
| `style_<カテゴリ名>.html`（18枚） | 各カテゴリの詳細ページ |
| `product_sample.html` | SELECT ITEM（デプロイ時は`product.html`にリネーム／デザインはエンジニア様オリジナル） |
| `g-select_sample.css` | 共有CSS（要・影響範囲確認） |
| `g-select_sample_pattern3-overrides.css` | TOPページ専用の上書きCSS（TOPページのみに影響） |
| `custom-nav.js` / `scroll-reveal.js` | ナビ開閉・スクロールフェード用JS |
| `images/` | 施工事例写真（`_originals_backup`を除く） |

### 対象外のファイル
`g-select_sample`フォルダ内には、検討段階の旧デザイン案（`index_sample.html`、`index_sample_pattern2.html`）も残っていますが、**今回の引き継ぎ対象はTOPページ = `index_sample_pattern3.html`のみ**です。それ以外の`index_sample*.html`や、フォルダ外にある古いプロトタイプファイルは無視してください。
